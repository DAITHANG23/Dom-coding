import React from "react";
import { Box } from "@mui/material";

const Posts = async () => {
  return (
    <Box
      sx={{
        height: "100vh",
        padding: "16px",
      }}
    >
      Posts
    </Box>
  );
};

export default Posts;
