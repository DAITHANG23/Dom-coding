import { Box } from "@mui/material";
import React from "react";

const Search = () => {
  return (
    <Box
      sx={{
        height: "100vh",
        padding: "16px",
      }}
    >
      Search
    </Box>
  );
};

export default Search;
